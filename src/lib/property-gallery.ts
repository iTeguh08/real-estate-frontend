/** Explore every angle gallery — exact count so each carousel page is a full 4-tile bento. */
export const PROPERTY_GALLERY_COUNT = 8;
/** Desktop bento — 4 tiles per carousel page (2 pages for 8 photos). */
export const PROPERTY_GALLERY_PAGE_SIZE = 4;
/** Mobile — hero + 2-col row only (max 2 rows); 3 pages for 8 photos. */
export const PROPERTY_GALLERY_MOBILE_PAGE_SIZE = 3;
